[project]
name = "tendor-poc"
version = "0.1.0"
description = "Document parsing comparison for procurement signal extraction"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pymupdf4llm",
    "docling",
    "langextract",
]

[tool.uv]
dev-dependencies = []